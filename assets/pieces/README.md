# Xiangqi SVG Pieces

Bộ 14 quân cờ vector, tự chứa, không phụ thuộc CDN:

- `red-*`: quân Đỏ
- `black-*`: quân Đen
- rook, knight, elephant, advisor, king, cannon, pawn

Mỗi file dùng `viewBox="0 0 128 128"` và có thể scale ở mọi độ phân giải.
